# fast-agent-stack — Invariants

Non-negotiable rules. The fas-gatekeeper agent BLOCKs any proposed change that violates these.

<!-- Example:
- I1: Every public function must have type annotations
- I2: No sync blocking calls in async paths
- I3: All errors must be typed, never bare Exception
-->
